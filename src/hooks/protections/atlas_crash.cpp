#include "hooking.hpp"
#include "util/notify.hpp"

namespace big
{
    //bool hooks::atlas_crash(int64_t a1, int64_t* a2)
    //{
    //    if (*(int64_t*)(a2 + 0x194) > INT32_MAX)
    //        return false;

    //    return g_hooking->get_original<hooks::atlas_crash>()(a1, a2);
    //}
}